# pair plot

```python
import seaborn as sns
import matplotlib.pyplot as plt

# To turn the interactive mode on
plt.ion()

help(plt.ion)

# Plot pairwise relationships in a dataset.
# There is a grid of plots.
# The diagonal contains univariate (single variable) distribution
sns.pairplot()


help(sns.pairplot)
```
